import { AppBar, Toolbar } from "@mui/material";

const NavBar = () => {
	return (
		<AppBar
			position="sticky"
		>
			<Toolbar>
				Ritrovo
			</Toolbar>
		</AppBar>
	);
}

export default NavBar;

